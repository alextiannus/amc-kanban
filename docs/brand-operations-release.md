# 品牌运营看板发布验证

状态：已部署。应用提交 `9c8fc4ef9305de5c100e6e38a0c2f0fe398d5894`。

- 服务：Render `amc-kanban-sg`（`srv-d94hhbt7vvec73dk7uig`）。
- 部署：`dep-danvo8cs728c73b75ggg`，2026-09-20 15:33:30 UTC 完成，状态 `live`。
- 入口：[主理人 → 品牌运营看板](https://amc-kanban.immedi.ai/board?tab=managementOverview)。
- 本次发布没有新增数据库迁移。原工作区独立的 PostFast key 回池改动没有包含在该提交。

## 审查修复

1. **P2：无请求时限导致界面永久等待。** 列表和保存请求现有 15 秒时限，涵盖响应正文读取。保存超时或结果不确定后要求刷新确认现状，防止盲目重复变更；409 版本冲突同样要求刷新。
2. **P2：失败后筛选显示与查询不一致。** 加载失败不再清空主理人筛选选项；选中的人选因归属变化不再出现在当前结果时仍保留可见选项。

## 验证结果

- 隔离发布目录的 `npm run build`（Next.js 16.2.9 Turbopack）、TypeScript、新文件 ESLint、Git diff 检查通过。
- `test:brand-operations`：月初/跨年时区边界、续约待激活选择、品牌范围、分页、管理员限制、OWNER 保护、过期版本拒绝及 PostgreSQL 事务回滚通过；增加请求超时、缓慢响应正文、结果不确定与 409/400 行为测试。
- 菜单分组、有效权限菜单、32 组权限总览组合回归通过；API 文档清单一致性通过。
- `scripts/test-brand-operations-ui.mjs` 在模拟 API 下验证桌面/手机、保存请求、空结果、失败重试、筛选保留、弹窗 Escape 和普通主理人只读；浏览器无运行时错误。
- 上线后正式域名首页返回 200；GET /api/brand-operations 与 PATCH /api/brand-operations/deployment-check/principal 在无会话时均返回 401、JSON、Cache-Control: no-store。

生产检查未执行真实品牌主理人变更；实际业务修改仍由管理员在页面中进行。生产认证后的完整品牌查询没有作为本次自动化冒烟测试执行，授权与数据聚合验证来自本地服务测试及模拟界面测试。

本记录与 PRD 状态更新是纯文档提交，使用 Render 支持的 `[skip render]` 提交标记，避免重复部署相同应用代码：[Render 部署文档](https://render.com/docs/deploys#skipping-an-auto-deploy)。

## 免收订阅费品牌排除（2026-09-20）

版本 `1cf5b07e951f355868c67a83f7c0a675dd077581`，部署 `dep-dao04i8jo6nc739t29b0` 于 15:59:46 UTC 进入 live。先按既有规则选合约，再排除 feeWaived=true 的品牌，列表、汇总、发布统计和主理人筛选一致。当前免收但有历史收费、最新免收的非生效订阅被排除；当前收费且存在历史免收或待激活免收续约仍保留。服务回归、类型、ESLint、API 文档检查和生产构建通过；上线后首页 200、未登录看板接口 401。未执行生产认证后的真实品牌列表查询。

## 紧凑只读布局（2026-09-21，Asia/Singapore）

版本 `045242d796337b7808686bc39a4a38b58f88dd7c`，部署 `dep-dao0dj7avr4c73aq7ap0` 于 2026-09-20 16:19:00 UTC 进入 live。表格单行显示、缩小行高，第一列仅品牌名和从位置明确识别的国家；未知国家显示 —。长名称截断，邮箱和合约开始日期以 title 提示保留。主理人编辑入口与弹窗已移除，原受保护接口保留。

当前 UI 回归已替换旧版本的编辑弹窗测试：管理员和主理人均无修改入口，零 PATCH 请求；桌面/手机、国家显示、单行样式、搜索与错误重试验证通过并检查截图。类型、ESLint、服务回归与生产构建通过；上线后首页 200、未登录接口 401。生产认证后的完整页面未自动检查。

## 点击品牌行打开编辑（2026-09-21）

版本 `776ecf0963404e42aa39aac0adfbbbc133f9cc59`，部署 `dep-daoa3pff3r2c73elretg` 于 03:20:56 UTC 进入 live。整行可点击，品牌名为真实链接；管理员直接展开 Admin 指定品牌编辑，非管理员进入已有权限保护的品牌策划页。无效品牌 ID 不回退到其他品牌。

模拟 API 的完整页面验证涵盖整行点击、正确 Admin 面板打开、普通主理人链接、桌面手机与原有筛选错误恢复。整页检查同时修复主理人指派区挤入底部按钮栏的问题，已移入滚动正文。类型、服务回归与生产构建通过；上线首页 200、未登录看板 API 401。未用生产登录身份编辑真实品牌。

## 编辑返回看板与隐藏品牌主（2026-09-21）

版本 `36d4877e907feb5cf677c6c048dd9236c6c60029`，部署 `dep-daoa93gjo6nc73a5s1g0` 于 03:32:05 UTC 进入 live。运营表格移除品牌主列，保留六列并缩小最小表格宽度。从看板带固定 returnTo=managementOverview 标记进入 Admin 编辑的用户，在关闭、取消、保存成功后返回运营看板；保存失败不退出；原生 Admin 入口关闭仍留在品牌管理。普通主理人的品牌策划页提供返回运营看板按钮。返回路径固定，不接受任意 URL。

完整页面模拟 API 回归验证六列、关闭、取消、成功保存返回、409 留在编辑及原生 Admin 不受影响；类型、服务回归与生产构建通过。上线首页 200、未登录看板接口 401，未修改生产业务数据。
